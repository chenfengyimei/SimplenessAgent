# SimplenessAgent 基础框架实现状态

> 状态：已验证的 P0/P1 基础纵向切片（2026-08-11）

## 本次完成范围

本次不是只创建目录，而是实现并通过测试的可运行核心：

| 基线能力 | 实现状态 | 位置 |
|---|---|---|
| 版本化领域契约 | 已完成 | `pkg/contracts` |
| SQLite Migration、WAL、外键 | 已完成 | `migrations`、`internal/storage` |
| Task/Step 物化状态与追加事件 | 已完成 | `internal/task`、`internal/eventstore`、`internal/storage` |
| 事务性状态迁移与事件序列 | 已完成 | `internal/storage` |
| Plan JSON DAG 与循环/依赖校验 | 已完成 | `internal/plan` |
| 授权工作区与路径逃逸阻断 | 已完成 | `internal/workspace` |
| 原子内容寻址 Artifact 与 Evidence | 已完成 | `internal/artifact`、`internal/storage` |
| 只读 Tool Registry（列目录/读文件/文本搜索） | 已完成 | `internal/tool` |
| Mock Provider 边界 | 已完成 | `internal/provider/mock` |
| CLI（初始化、工作区、任务、运行、查询） | 已完成 | `cmd/simpleness` |
| 侦察任务端到端闭环与重启读取 | 已完成 | `tests/integration`、`tests/recovery` |
| Wails/Vue 桌面工作台 | 计划中 | P5 |
| OpenAI-compatible/本地 Runtime Provider | 计划中 | P0.2/P1.4 |
| Agent Worker、写工具、审批与幂等恢复 | 计划中 | P1.4-P1.6 |
| Checkpoint 恢复审计、局部重规划、上下文编译 | 计划中 | P2 |
| 记忆、Skill、多 Agent、评测中心 | 计划中 | P3-P4 |

## 已验证的运行链

`CLI → App Service → SQLite 事务（Task + Event） → Plan DAG → Workspace 边界内只读 Tool → Artifact + Evidence → Step/Task 验证完成 → Checkpoint → 重启读取快照`

这条链路满足“任务、计划、证据和恢复独立于 UI 与聊天记录”的基础决策。当前执行器故意只运行确定性的侦察步骤；模型或副作用尚未接入，因此不会发生未经授权的文件修改。

## 下一阶段开发事项

1. **P0.2 / Provider PoC**：实现 OpenAI-compatible Provider 的文本、流式、Tool Call、取消与能力探测；加入脱敏原始响应夹具。
2. **P1.4 / Agent Worker**：实现固定 System Contract、受控单工具循环、Schema 校验、重复动作与预算停止条件；以 Mock Provider Golden Cases 驱动。
3. **P1.5 / 可写工具**：先完成 Tool Intent、参数哈希、审批票据和幂等恢复测试，再开放 `apply_patch`、受控 `write_file` 与命令工具。
4. **P1.6 / 验证器**：补齐命令、文件和 Diff 断言，生成 FinalReport，禁止无 Evidence 的完成结论。
5. **P2 / 中长任务能力**：Step 边界 Checkpoint、恢复审计、Plan Revision、局部重规划和 Context Budget。
6. **P3-P5**：在核心闭环稳定后实施 FTS5 记忆/项目索引、Skill、单层多 Agent，最后接入 Wails + Vue 任务工作台。

## 明确的范围边界

- 基础框架不把未完成能力标称为“支持”。
- 任何写、危险、网络或外部副作用均保持 fail-closed。
- 前端状态不能成为任务事实来源；后续 UI 仅通过 App Service 的命令/查询与事件流交互。
